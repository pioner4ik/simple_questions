$('.flash-messages').html("<%= j render "partials/flash" %>");

<% if @answer.errors.any? %>
  $('.new_answer').prepend("<%= j render "partials/errors", obj: @answer %>");
<% else %>
  $('.answers').append('<%= j render @answer %>');
  $('textarea#answer_body').val('');
<% end %>