$('.flash-messages').html("<%= j render "partials/flash" %>");

<% if @answer.errors.any? %>
  $('#answer-' + <%= @answer.id %> ).before("<%= j render "partials/errors", obj: @answer %>");
<% else %>
  $('#answer-' + <%= @answer.id %> ).replaceWith("<%= j render @answer %>");
<% end %>



